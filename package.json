{
  "name": "maxwebdata-zap-app-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "$(npm bin)/jest --testTimeout 10000",
    "coverage": "$(npm bin)/jest --testTimeout 10000 --coverage",
    "lint": "$(npm bin)/eslint . --ext .js",
    "zapier": "$(npm bin)/zapier"
  },
  "dependencies": {
    "zapier-platform-core": "12.0.3"
  },
  "devDependencies": {
    "eslint": "^8.16.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-jest": "^26.2.2",
    "jest": "^26.6.3",
    "zapier-platform-cli": "^12.0.3"
  },
  "private": true
}
